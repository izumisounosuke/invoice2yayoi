# 制作物のご説明

## 作ったもの

請求書の写真から、会計ソフト（弥生会計 Next）にそのまま取り込める
**仕訳インポートCSV**を生成するツールです。

読み取り結果をJSONで返すところで止めず、勘定科目と税区分を判定し、
会計ソフトが受け取れる形式まで変換して、実際に仕訳として並ぶところまでを
動画に収めています。

---

## なぜこのテーマを選んだか

中小企業で経理を一人で担当しており、請求書の処理を毎月行っています。

その立場から見て、OCRを業務に入れるときに詰まるのは読み取りの精度ではなく、
**読み取った後に何をするか**でした。

請求書から「取引先」「日付」「金額」を取り出せても、経理が実際に必要なのは
仕訳です。仕訳にするには、その支出をどの勘定科目に入れるか、税率が8%なのか
10%なのか、適格請求書なのかどうかを決める必要があります。この判断は
OCRの守備範囲の外にありますが、ここが決まらないと1件も入力が減りません。

「読み取って終わり」ではなく「その後の手作業がどれだけ消えるか」を
見せられるものにしたい、というのが最初に考えたことです。

---

## 実装で意識した3点

### 1. 税率ごとに仕訳を分ける

合計金額が合っていても、8%と10%を混ぜて1本の仕訳にすると申告額がずれます。
明細を（勘定科目 × 税率）で束ね、税率ごとに別の仕訳として出しています。

軽減税率の判定は、※印での区分がある書類ではそれを最優先しています。
品名からの推測より、書類自体に印字された区分の方が確かな根拠になるためです。

### 2. 判定できないものは埋めない

品名から勘定科目を判定できなかった行は、それらしい科目を推測で入れず、
仮払金に置いて要確認として出します。推測で埋めた方が見た目は良くなりますが、
後から間違いを見つけにくくなります。

### 3. 宣言できる制約は宣言する

登録番号に `pattern: ^T\d{13}$` を宣言しています。適格請求書発行事業者の
登録番号はT＋数字13桁と決まっているので、`TO000000000002`（2文字目がO）の
ような誤読は、文字照合の一致率を見るまでもなく落とせます。

一方で、品名・単位・備考には `review: "off"` を付けました。品名が「WF」でも
「VVF」でも仕訳は同じように切れます（摘要欄に入る情報なので実害がない）。
ここで要確認を出すと、登録番号や税額に立った印が埋もれます。

この2つを入れた結果、同じ5枚での要確認件数が **20件から9件** に減りました。
落ちたのは値が正しいのに上がっていたものだけで、実際の誤りは残っています。

---

## 動画で見ていただきたい点

デモ用に6枚目のダミーを追加しました。**登録番号のない請求書**です。
免税事業者の下請けや個人事業主からの請求では実際に頻出します。

動画では15件の仕訳が並びますが、**摘要に【要確認】が付いているのは
この1件だけ**です。読み取りは全て正しく、金額も合っている。それでも
この請求書は適格請求書ではないので、仕入税額控除の扱いが変わります。

実務で本当に困るのは読み違いより、こうした書類を気づかず処理してしまう
ことです。読み取り精度とは別の軸で「人が見るべき場所」を出せるところに、
このAPIを業務に入れる意味があると考えました。

---

## 検算

CSVを出力する前に、次の4つを確認しています。

- 数量 × 単価 = 金額（明細行ごと）
- 対象額 + 消費税 = 合計
- 税率ごとの明細合計 = 対象額
- 作成した仕訳の合計 = 請求合計

APIが返す要確認とは別に、こちら側で計算が成り立つかを見る層です。
最初の検算は、実際に数量の誤読（4を44と読む）を検出できました。

---

## 納品物

| ファイル | 内容 |
|---|---|
| `main.py` ほか `.py` | ソースコード一式 |
| `README.md` | 使い方と設計の説明 |
| `docs/01_overview.md` | 本書（制作物の説明） |
| `docs/02_feedback.md` | 制作を通して気づいた点 |
| `demo/demo_yayoi_import.mp4` | 取り込みの動作（27秒） |
| `demo/demo_yayoi_import.gif` | 同内容のGIF |
| `invoices/` | 検証に使ったダミー請求書6枚 |

Python 3の標準ライブラリのみで動きます。APIキーは環境変数から読み、
ソースコードには含めていません。

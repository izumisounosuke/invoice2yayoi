# 制作を通して気づいた点

「良い評価をお願いするものではない」とのことでしたので、率直に書きます。
実装しながら実際に詰まった順に並べています。

---

## 1. 型宣言と日本の請求書の書式が合わない場面がある

支払期限に `type: "date"` を宣言したところ、正常な請求書が
`type_mismatch` で要確認に上がりました。印字が **「2026年9月末日」**
だったためです。

日本の請求書では、支払期限に「月末日」「翌月末払い」「締日から60日」
といった、日付として解釈できない表記が普通に使われます。実務では
これらは不備ではなく、むしろ一般的な書き方です。

結果として `type: "string"` に戻しました。宣言の仕様としては正しい挙動で、
ドキュメントにも書かれているとおりです。ただ、**「型を宣言すると
要確認が増える」という副作用が、日付項目では特に出やすい**という
注意があると、同じところで迷う人が減ると思います。

ドキュメントの `type` の説明に、「業務上は正しいが型として解釈できない
表記がある項目（支払期限、数量の『一式』など）には宣言しない方がよい」
という一文があると助かります。

---

## 2. label が効く条件を、混在書類でどう満たすか分からなかった

`label` は「ページに正確に1回だけ印字されているとき」に働くとありました。

税率が混在する請求書では、「消費税」という文字列が
「消費税（8%）」「消費税（10%）」として2回出ます。この場合、

- `label: "消費税"` は2回出るので効かない
- `label: "消費税（8%）"` は完全一致が必要か、部分一致でよいのか

が読み取れませんでした。今回は配列で候補を複数渡しましたが、
**配列を渡したとき「候補のどれか1つが1回だけ出ていればよい」のか、
「渡した候補すべてが条件を満たす必要がある」のか**が分からず、
挙動を確認しないまま進めています。

ドキュメントに「候補が複数あるときは配列で」とあるので前者だと思いますが、
明示されていると安心して使えます。

---

## 3. 明細行に label が効かないのが、軽減税率で効いてくる

トップレベルの string フィールド専用という制約は理解しています。

ただ、軽減税率の書類では **「※印がどの行に付いていたか」を座標で
追えると助かります**。税務調査で「この仕訳がなぜ8%なのか」を示す場面が
実際にあり、そのときに原本のどこを見ればよいかが分かる形だと、
説明の手間がかなり違います。

明細行そのものではなく、行内の特定の子フィールドを、その行の中で
アンカーできる仕組みがあると使いどころがありそうです。

---

## 4. 登録番号が、ほぼ全ての書類で要確認に上がった

ダミー6枚のうち5枚で、登録番号が `low_ocr_confidence` /
`text_mismatch` / `ambiguous_occurrence` のいずれかで要確認に
上がりました。値はいずれも印字どおりです。要確認10件のうち6件が
登録番号に関するものでした。

ダミーの登録番号を `T0000000000000` のようにゼロ連続で作ったことが
原因だと思うので、実際の番号ではここまで出ないかもしれません。

ただ、こうなると実務では「登録番号はいつも出るから見なくていい」という
運用に落ち着きます。そのうちに、本当に誤っているときも見落とされます。

今回は `pattern` を宣言していたので、形式違反なら別の理由
（`pattern_mismatch`）で上がると分かっており、信頼度由来の warn は
無視して問題ないと判断できました。ただ、宣言していなければ
判断がつかなかったと思います。

そこで提案なのですが、**`pattern` を宣言して通過した項目は、
OCR信頼度由来の warn を一段下げる**という選択肢があってもよいと
感じました。形式が保証されている以上、信頼度の低さが示す危険は
小さくなるためです。

現状は「宣言違反はエンジンの推定より上位」というランク付けですが、
逆方向（宣言を満たしたことが、推定を弱める根拠になる）は
無いようでした。件数を減らすためというより、印を意味のあるものに
保つという点で効いてくると思います。

---

## 良かった点

不満ばかりでも参考にならないと思うので、実装が楽だった部分も書きます。

**`normalized` が `values` と同じ形のツリーで返る**設計は扱いやすかったです。
印字されたままの値と、計算に使える値を、同じパスで並べて持てるので、
「表示は原本どおり、計算は数値」という分け方がそのまま実装できました。

**`review: "off"` のフィールド単位指定**は、期待どおりに効きました。
品名と備考に付けただけで要確認が20件から9件に減り、残ったものは
確認する価値のあるものだけになりました。これが無いと、実務では
一覧そのものが見られなくなっていたと思います。

**エラーコードの切り分けが明記されている**のも助かりました。
特に「400 invalid_image は再試行しても変わらない / 502・504 は
再試行の価値がある」という記述があったおかげで、リトライの実装で
迷いませんでした。この粒度で書かれているAPIドキュメントは多くありません。

---

## 再現性について

「同じ写真でも読み直すと結果が変わる」点は、以前お伝えしたとおり
実務では重い制約です。今回は読み取り結果をJSONで保存し、
2回目以降は再利用する形にして回避しました。

ドキュメントの目立つ場所に「結果は保存して確定値として扱うこと」という
記述があると、後から気づいて作り直す人が減ると思います。
